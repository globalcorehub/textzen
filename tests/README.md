# Audit regression tests

Run `node tests/audit-regression.cjs` with Playwright installed. Existing installations may be selected via `PLAYWRIGHT_MODULE` and `CHROMIUM_PATH`. These tests serve the actual Worker locally, use synthetic inputs, block third-party requests and activate buttons by keyboard.
